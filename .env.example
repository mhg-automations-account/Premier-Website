# Enables live Google reviews in the Testimonials section.
# Create a key in Google Cloud Console with the "Places API (New)" enabled,
# restricted to server-side use (no HTTP referrer restriction needed since
# it's only called from the server). Without this, the section falls back
# to sample reviews.
GOOGLE_PLACES_API_KEY=
